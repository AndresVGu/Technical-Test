spring.application.name=technicalTest

#======================
#DATASOURCE [H2] (LOCAL)
#======================
#spring.datasource.url=jdbc:h2:mem:superdb;MODE=MySQL;DB_CLOSE_DELAY=-1
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=kali
#spring.datasource.password=
#para evitar que en produccion se epongas los datos de auteticacion, se usan variables de entorno
#========================
# JPA / HIBERNATE
#========================
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

#Para que H2 se comporte como MySQL (importante para tests)
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect


#======================
# H2 CONSOLE
#======================
#spring.h2.console.enabled=true
#spring.h2.cosole.path=/h2-console

# ==========================
# Configuraciones de JPA + MySQL (Produccion)
# ==========================
#Variables de entorno DB_URL, DB_USER_NAME,DB_PASSWORD
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=${DB_PLATFORM}
server.port=8080